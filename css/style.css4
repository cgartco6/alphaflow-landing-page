@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Montserrat:wght@600;700;800&display=swap');

:root {
  --black: #0B0B0B;
  --dark: #1C1C1C;
  --gold: #C8A24A;
  --gold-light: #E8C46A;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: 'Inter', system_ui, sans-serif;
  background: var(--black);
  color: #e5e5e5;
  line-height: 1.7;
}

.heading-font {
  font-family: 'Montserrat', sans-serif;
  font-weight: 800;
  letter-spacing: -0.04em;
}

.gold { color: var(--gold); }

/* Navbar */
nav {
  position: fixed;
  top: 0;
  width: 100%;
  background: rgba(11, 11, 11, 0.98);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid #333;
  z-index: 1000;
}

.nav-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 1.25rem 1.5rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.logo {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 1.8rem;
}

.hamburger {
  display: none;
  font-size: 1.9rem;
  cursor: pointer;
  color: white;
  background: none;
  border: none;
}

.nav-links {
  display: flex;
  gap: 2.2rem;
  align-items: center;
  font-weight: 500;
}

.nav-links a {
  color: #ddd;
  transition: color 0.3s;
}

.nav-links a:hover { color: var(--gold); }

/* Hero */
.hero {
  min-height: 100vh;
  background: linear-gradient(rgba(11,11,11,0.88), rgba(11,11,11,0.92)), url('https://picsum.photos/id/1015/2000/1400') center/cover no-repeat;
  display: flex;
  align-items: center;
  text-align: center;
}

.hero-content {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 1.5rem;
}

.hero h1 {
  font-size: clamp(3.2rem, 8.5vw, 5.8rem);
  line-height: 1.05;
  margin-bottom: 1.5rem;
}

.hero p {
  font-size: 1.55rem;
  max-width: 720px;
  margin: 0 auto 2.8rem;
  color: #ccc;
}

/* Buttons */
.btn-primary {
  background: linear-gradient(90deg, var(--gold), var(--gold-light));
  color: #0B0B0B;
  font-weight: 700;
  padding: 18px 42px;
  border-radius: 9999px;
  font-size: 1.15rem;
  transition: all 0.4s ease;
  box-shadow: 0 10px 30px rgba(200,162,74,0.35);
}

.btn-primary:hover {
  transform: translateY(-4px);
  box-shadow: 0 15px 40px rgba(200,162,74,0.45);
}

.btn-secondary {
  border: 2px solid var(--gold);
  color: white;
  padding: 16px 42px;
  border-radius: 9999px;
  font-weight: 600;
  transition: all 0.3s;
}

.btn-secondary:hover {
  background: rgba(200,162,74,0.12);
}

/* Bundle */
.bundle-card {
  background: var(--dark);
  border: 3px solid var(--gold);
  border-radius: 32px;
  padding: 3.5rem 2.5rem;
  position: relative;
  max-width: 540px;
  margin: 0 auto;
}

.bundle-card::after {
  content: "BEST VALUE • SAVE R199";
  position: absolute;
  top: -18px;
  right: 40px;
  background: var(--gold);
  color: #0B0B0B;
  font-weight: 800;
  padding: 8px 26px;
  border-radius: 9999px;
  font-size: 0.95rem;
}

/* Mobile Hamburger */
@media (max-width: 768px) {
  .hamburger { display: block; }
  .nav-links {
    position: fixed;
    top: 80px;
    left: -100%;
    width: 100%;
    height: calc(100vh - 80px);
    background: var(--black);
    flex-direction: column;
    padding: 3rem 2rem;
    gap: 2rem;
    transition: left 0.4s ease;
  }
  .nav-links.active { left: 0; }
}
